var array = [1,2,3,4,5,6];

function Iterar(array) {

    var contador = 0;

        while (contador <= array.length){
                console.log (array[contador])
            contador++
        }
  
}

Iterar(array);